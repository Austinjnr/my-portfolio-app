import React, {useState} from "react";

function Register() {
    return (
        <section>
              <h1>form to sign in and register</h1>
        </section>
      );
}

export default Register;